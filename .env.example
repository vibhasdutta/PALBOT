DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
# Reachable hostname or IP only, no scheme/port -- e.g. 20.207.201.17 or
# your-domain.com. The bot builds http://<WEB_HOST>:<WEB_PORT> itself, so
# the port never needs to be typed in two places.
WEB_HOST=localhost
WEB_PORT=8090
WEB_SECRET=
